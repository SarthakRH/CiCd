// backend/tests/backend.test.js

// Import any necessary modules or functions from your backend code
// For example, if you have an `add` function in your backend, you can import it like this:
// const { add } = require('../controllers/admin/user');

test('Example test case', () => {
    // Your test code goes here
    // Use Jest's expect function to make assertions
    expect(2 + 3).toBe(5);
  });
  